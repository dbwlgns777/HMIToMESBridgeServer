spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ZES_Authentication?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mybatis/*.xml
  type-aliases-package: com.zes.bridge.model
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080
